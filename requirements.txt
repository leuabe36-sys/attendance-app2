Flask==3.1.0
Werkzeug==3.1.3
opencv-python-headless==4.10.0.84
mediapipe==0.10.18
numpy==1.26.4
psycopg2-binary==2.9.10
requests==2.32.3

# IMPORTANT: mediapipe is pinned to 0.10.18 deliberately, not left open.
# This app uses the legacy `mediapipe.solutions.face_mesh` API. Newer
# mediapipe releases (0.10.2x+, and the 1.0.0 line) have DROPPED
# `mediapipe.solutions` entirely in favor of the newer Tasks API, so
# upgrading this pin will break every face-recognition route with
# `AttributeError: module 'mediapipe' has no attribute 'solutions'`.
gunicorn==23.0.0

